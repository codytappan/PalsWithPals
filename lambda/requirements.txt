# Lambda runtime dependencies.
#
# PyNaCl is required for Ed25519 signature verification and must be bundled
# into the deployment package (it is not in the Lambda runtime).
pynacl>=1.5.0

# boto3 is already provided by the Python 3.12 Lambda runtime; listed here for
# documentation and local development.
boto3>=1.34.0
